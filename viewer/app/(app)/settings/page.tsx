import { PhasePlaceholder } from "../_placeholder";
export default function SettingsPage() {
  return (
    <PhasePlaceholder
      title="설정"
      desc="탐지 규칙 임계값, 경쟁 브랜드 리스트, 프롬프트 버전을 관리합니다."
    />
  );
}
